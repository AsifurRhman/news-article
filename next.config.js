/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:["example.com","lh3.googleusercontent.com","avatars.githubusercontent.com"],
    }
}

module.exports = nextConfig
